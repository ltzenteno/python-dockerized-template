[tool.poetry]
name = "mj platform"
version = "0.1.0"
description = "Platform for something at ITESM"
authors = ["Luis Zenteno"]

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=1.0"]
build-backend = "poetry.masonry.api"
